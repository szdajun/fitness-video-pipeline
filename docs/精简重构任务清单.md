# 健身视频优化流水线精简重构任务清单

## 使用说明

这份清单对应 [重构路线精简版.md](/F:/wkspace/fitness-video-pipeline/docs/重构路线精简版.md)，只保留当前阶段最值得立即投入的改造项：

1. Manifest 增量恢复
2. H2V 策略拆分
3. 轻量 Metrics
4. 更新过时文档
5. 整理根目录与 `.gitignore`

本轮暂缓：

- 配置大一统
- Stage requires/provides 契约体系
- Context 分层

---

## 第一优先级：Manifest 增量恢复

### 目标

让中间产物恢复从“猜文件名”升级为“读 manifest”。

### 任务清单

- [ ] 新建 `pipeline/manifest.py`
- [ ] 设计 manifest 文件结构
- [ ] 记录 input path
- [ ] 记录 input mtime
- [ ] 记录 input size
- [ ] 记录 config hash
- [ ] 记录 cache version
- [ ] 为每个 stage 记录 outputs
- [ ] 实现 `get_manifest_path()`
- [ ] 实现 `load_manifest()`
- [ ] 实现 `save_manifest()`
- [ ] 实现 `build_input_fingerprint()`
- [ ] 实现 `is_manifest_compatible()`
- [ ] 实现 `restore_context_from_manifest()`
- [ ] 实现 `record_stage_result()`
- [ ] 在 engine 启动时读取 manifest
- [ ] 在 engine 启动时校验 manifest 可用性
- [ ] 在 stage 成功后写回 manifest

### 首批接入 stage

- [ ] `pose_detect`
- [ ] `h2v_convert`
- [ ] `body_warp`
- [ ] `color_grade`
- [ ] `ken_burns`

### 第二批优先候选

- [ ] `beat_flash`

### `pose_detect` 额外任务

- [ ] keypoints cache 写入 `cache_version`
- [ ] keypoints cache 写入 `config_hash`
- [ ] keypoints cache 写入 `input_mtime`
- [ ] keypoints cache 写入 `model_name`
- [ ] 命中缓存时恢复 `keypoints`
- [ ] 命中缓存时恢复 `video_info`
- [ ] 命中缓存时恢复 `keypoints_path`

### `h2v_convert` 额外任务

- [ ] 命中已有产物时恢复 `h2v_path`
- [ ] 命中已有产物时恢复 `cropped_keypoints`
- [ ] 命中已有产物时恢复 `cropped_keypoints_path`
- [ ] 命中已有产物时恢复 `h2v_size`

### `body_warp` 额外任务

- [ ] 命中已有产物时恢复 `warped_path`

### `color_grade` 额外任务

- [ ] 命中已有产物时恢复 `color_path`

### `ken_burns` 额外任务

- [ ] 命中已有产物时恢复 `ken_burns_path`
- [ ] 命中已有产物时恢复 `ken_burns_ratio`

### 验收

- [ ] 同一输入第二次运行时关键 stage 稳定跳过
- [ ] 改参数后旧缓存不会被误复用
- [ ] 删除中间文件后不会错误恢复
- [ ] `ken_burns` 重跑次数明显下降

---

## 第二优先级：H2V 策略拆分

### 目标

把 `03_h2v_convert.py` 中的策略决策逻辑拆到独立模块，降低后续调参与维护成本。

### 任务清单

- [ ] 新建 `lib/crop_strategy.py`
- [ ] 抽出 `build_tracks()`
- [ ] 抽出 `score_track()`
- [ ] 抽出 `select_lead_track()`
- [ ] 抽出 `classify_frame()`
- [ ] 抽出 `classify_frames()`
- [ ] 抽出 `merge_segments()`
- [ ] 抽出裁切计划相关函数

### `03_h2v_convert.py` 结构整理

- [ ] 抽出 `_load_inputs()`
- [ ] 抽出 `_compute_base_crop()`
- [ ] 抽出 `_build_scene_plan()`
- [ ] 抽出 `_render_segments()`
- [ ] 抽出 `_save_cropped_keypoints()`

### 优先配置化的常量

- [ ] `top_region_ratio`
- [ ] `pano_person_threshold`
- [ ] `min_segment_seconds`
- [ ] `lead_crop_padding`
- [ ] `multi_crop_width_ratio`
- [ ] `track_match_threshold`

### 原则

- [ ] 第一版只拆结构，不大改算法
- [ ] 第一版尽量保持输出行为接近当前版本
- [ ] 先把策略从执行逻辑中分离，再考虑进一步优化效果

### 验收

- [ ] `03_h2v_convert.py` 明显变短
- [ ] 人物追踪/领操人识别/场景判定/分段合并已迁出
- [ ] H2V 关键参数可以独立调节
- [ ] 重构前后结果大体一致

---

## 第三优先级：轻量 Metrics

### 目标

为每次运行输出结构化指标，降低纯肉眼评估的依赖。

### 任务清单

- [ ] 新建 `lib/quality_metrics.py`
- [ ] 输出 `run_metrics.json`
- [ ] 记录 `pose_detect_rate`
- [ ] 记录 `avg_person_count`
- [ ] 记录 `lead_center_jitter`
- [ ] 记录 `output_frame_delta`
- [ ] 记录 `audio_video_duration_delta`
- [ ] 记录 `video_duration_sec`

### 可选后续项

- [ ] 记录 `ankle_out_of_frame_rate`
- [ ] 记录 `crop_center_drift`

### 验收

- [ ] 每次主流程结束后生成 `run_metrics.json`
- [ ] 不同参数或 preset 的输出结果可以量化比较
- [ ] 后续优化 H2V 时有结构化反馈

---

## 第四优先级：更新过时文档

### 目标

让项目文档和当前实际 pipeline 对齐。

### 任务清单

- [ ] 更新 [视频生成流程.md](/F:/wkspace/fitness-video-pipeline/docs/视频生成流程.md)
- [ ] 标明当前主入口是 `main.py`
- [ ] 标明当前主流程是多 stage pipeline
- [ ] 标注旧脚本与现流程的关系
- [ ] 标注哪些文档只作历史参考

### 验收

- [ ] 新维护者阅读流程文档后不再误解当前架构

---

## 第五优先级：整理根目录与 `.gitignore`

### 目标

降低根目录噪音，避免调试残留和输出文件干扰版本管理。

### 任务清单

- [ ] 新建或使用 `tools/` 目录
- [ ] 将一次性脚本逐步迁入 `tools/`
- [ ] 清理或忽略 `check_*.jpg`
- [ ] 清理或忽略 `dbg_*.jpg`
- [ ] 清理或忽略其他调试图片
- [ ] 将 `output/` 加入 `.gitignore`
- [ ] 将 `__pycache__/` 加入 `.gitignore`
- [ ] 视情况忽略 `*_keypoints.json`

### 验收

- [ ] 根目录明显更干净
- [ ] 版本控制不再混入大量输出和缓存

---

## 建议推进顺序

- [ ] 先完成 Manifest
- [ ] 尽早把 `ken_burns` 接入 Manifest
- [ ] `beat_flash` 作为同批可选或第二批优先
- [ ] 再拆 H2V 策略
- [ ] 最后补 Metrics
- [ ] 主线稳定后更新文档和整理目录

---

## 每一阶段后的最小回归

### Manifest 完成后

- [ ] 同一视频连续运行两次
- [ ] 验证关键 stage 是否正确跳过
- [ ] 改一个参数后验证是否重新执行

### H2V 拆分后

- [ ] 用同一输入对比拆分前后输出
- [ ] 检查领操人裁切是否明显跑偏
- [ ] 检查分段是否明显异常

### Metrics 完成后

- [ ] 确认 `run_metrics.json` 能落盘
- [ ] 确认指标字段齐全
- [ ] 确认指标数值大致合理

### 文档与目录整理后

- [ ] 确认文档描述与现流程一致
- [ ] 确认根目录无明显调试垃圾残留

---

## 本轮不做

- [ ] 不做全量配置 schema 迁移
- [ ] 不做全量 preset 重写
- [ ] 不做完整 stage 契约系统
- [ ] 不做 context 强分层

这几项不是永远不做，而是当前阶段投入产出比不高，暂缓更合适。
