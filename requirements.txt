opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0
filterpy>=1.4.5
ultralytics>=8.0.0
mediapipe>=0.10,<0.11
insightface>=0.7.3
onnxruntime-gpu>=1.19.0  # GPU 加速换脸; 借 torch 自带 cuDNN, 无 GPU 时 provider 自动 fallback CPU

# Optional GPU acceleration
# torch>=2.0.0
# torchvision>=0.15.0
