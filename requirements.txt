# Python 依赖 — uv 优先管理 (uv sync --extra dev), 本文件兼容 pip install -r.
# 权威源: pyproject.toml [project.dependencies] + uv.lock (可复现).
# Python >=3.11 (3.9 已 EOL 2025-10; uv 按 .python-version 管 Python 版本).
# torch (GPU, RVM 抠像用) 不在此 — 装: uv pip install torch --index-url https://download.pytorch.org/whl/cu121
opencv-python>=4.8.0
numpy>=1.24,<2
pyyaml>=6.0
filterpy>=1.4.5
scipy>=1.10
pillow>=9.0
ultralytics>=8.0.0
mediapipe>=0.10,<0.11
insightface>=0.7.3
onnxruntime-gpu>=1.19.0
pytest>=8.0  # dev only
